# VESTIS AI 更新紀錄與 Bug 修復報告 (2026/06)

本文件記錄了 VESTIS AI 專案近期針對「Web 推薦單品美學與導購升級」、「本機圖片 CORS 代理服務與死鎖修復」、「上流奢華兩欄式版面設計」、「一鍵多圖快速合成 & 自動化回傳穩定性」所完成的重大核心架構更新與 Bug 修復。

---

## 🚀 最新更新項目 (2026-06-18 版面優化、免註冊體驗額度防重複扣除與 Google 登入修復)

### 1. 模特兒切換箭頭內縮與操作面板復原
* **版面美化**：將 `trial-actions-panel` 重新移回**左下角**（`left: 1.25rem`，並改為靠左排列），避免置於底部正中央時遮擋主模特兒的穿搭服裝細節。
* **按鈕防遮擋**：將 desktop 版的左右模特兒切換箭頭（`.model-switch-left` 與 `.model-switch-right`）由原本的外懸狀態（`-25px`）改為**向模特兒主體內縮**（`10px`），確保不會被左下角的 `trial-actions-panel` 操作面板遮擋，操作更為流暢。

### 2. 免註冊體驗額度防重複扣除鎖定 (Guest Credits Duplicate Lock)
* **防重複處理鎖**：新增了 `isProcessingSynthesizedImage` 狀態鎖。當「一鍵多圖自動合成」完成時，無論是透過 postMessage 還是本地 API 輪詢獲取結果，皆只會執行一次 `loadSynthesizedImageFromBase64` 處理，從而**確保每次自動合成僅會精準扣除 1 次體驗額度**。
* **額度防洩漏**：徹底修復了免註冊體驗只進行一次 AI 合成便因多通道重複扣減而耗盡所有額度（3 次）的重大 bug，訪客體驗更為友善。

### 3. Google 帳戶登入重定向與錯誤排除 (Supabase OAuth Whitelist Optimize)
* **重定向路徑優化**：將 Google 登入的 `redirectTo` 由 `window.location.origin + window.location.pathname`（例如 `/index.html`）優化為**根路徑** `window.location.origin + "/"`。此舉可極大程度地避免因未將帶有特定檔名之完整路徑手動加入 Supabase Redirect URLs 白名單而觸發的 `redirect_uri_not_allowed` 錯誤。
* **錯誤提示與指引**：在 Google 登入按鈕中加入 Supabase 連線狀態判斷，如未成功連線或後台 Google Provider 未啟用，會主動以 Toast 顯示引導說明（「請至 Supabase 控制台的 Auth -> Providers 開啟 Google 登入功能」），便於開發者排查。

---

## 🚀 歷史更新項目 (2026-06-16 VESTIS AI 雲端網頁版 Supabase 整合 — Phase 1)

### 1. 訪客免註冊體驗模式 (Guest Mode)
* **體驗優先**：支援訪客在不註冊登入的情況下，直接體驗 3 次 AI 擬真渲染或 Gemini 穿搭合成。
* **次數限制**：透過 `localStorage.vestis_guest_credits` 進行訪客試穿次數扣除與防呆阻擋，額度扣完後會自動導引至會員中心。
* **資料本地化**：訪客單品、收藏與日誌均暫存在本機 `localStorage`，保障即開即玩。

### 2. Atelier 會員中心 (Google & Email 雙模態驗證)
* **新分頁整合**：在左側邊欄與手機底部導航欄中新增「🔑 會員」分頁。
* **雙通道驗證**：對接 Supabase Auth，支援「Email/密碼」的快速註冊與登入，並整合「Google 帳戶一鍵登入 (OAuth)」。
* **雲端資料自動同步**：使用者登入後，系統會自動將訪客期間上傳的單品同步至雲端，並從雲端資料庫加載使用者專屬的衣櫃、收藏、日誌與數位分身。

### 3. Supabase 雲端資料庫與安全 RLS 保護 (Serverless SQL)
* **自動分身 trigger**：實作 PostgreSQL 觸發器，使用者註冊時自動於 `profiles` 表中建立預設分身欄位。
* **安全行級鎖 (RLS)**：所有使用者資料表皆啟用行級安全策略 (Row Level Security)，使用者僅能存取與操作屬於自己 `auth.uid()` 的單品、日誌與收藏，防範越權漏洞。

### 4. 雲端儲存桶隔離 (Supabase Storage)
* **單品與分身圖片 CDN 化**：上傳自訂單品或生成數位分身時，Base64 圖片會自動上傳至 Supabase Public `wardrobe` 儲存桶中，並自動按使用者 ID `userId/filename` 分離存放，將生成的 HTTP 公開 URL 存回資料庫。
* **防盜鏈與越權防禦**：Storage 僅允許 Authenticated 擁有者對其專屬資料夾寫入與刪除，一般訪客僅能 select 讀取公開連結。

### 5. Deno Serverless AI 安全代理保護 (vton-proxy)
* **金鑰保護**：建立 Supabase Edge Function `/vton-proxy`，在伺服器端讀取環境變數 `FAL_API_KEY`，前端 executeVTON 調用全面轉由 Edge Function 進行代理，徹底解決 `FAL_API_KEY` 在前端流出暴露的安全性隱患。
* **向下相容**：若無 Supabase 金鑰或在本地模式下，executeVTON 會自動 fallback 回原本的 client-side API 直接調用，確保本地離線開發依然可用。

### 6. 雲端 Gemini 自動化合成會話控制
* **雲端會話同步**：Gemini 智慧合成調用時，改為 upsert 雲端 `gemini_sessions` 表。同時，背景 Gemini 自動化視窗載入時會自動透過 URL 參數 `sb_url`, `sb_key`, `user_id` 與 Supabase 建立連線，讀取 Payload 並 PATCH 寫回 completed base64 結果，完美支援雲端背景無感合成！

---

## 🚀 歷史更新項目 (2026-06-16 「個人衣櫃」單品刪除與背景自動合成防休眠優化)

### 1. 個人衣櫃所有單品刪除功能 (含預設與自訂單品)
* **功能完整性**：現在個人衣櫃中所有的衣物（包括使用者自己上傳的自訂單品與系統內建的原有預設單品）均支援刪除功能。
* **持久化過濾**：
  - 自訂單品繼續從 `localStorage.vestis_custom_wardrobe` 中物理移除。
  - 新增 `localStorage.vestis_deleted_default_items` 機制。當用戶刪除內建單品時，其 ID 會被記錄下來，網頁初始化時會自動進行排除過濾。這確保了重新載入網頁後，被刪除的預設單品依然保持移除狀態。
* **代碼整合去重**：卡片右上角「✕」按鈕、詳情彈窗的「🗑️ 刪除單品」按鈕、以及拖曳到垃圾桶（`trash.ondrop`）均統一呼叫全域 `deleteWardrobeItem(id)`，提升代碼健壯性。

### 2. 背景自動合成防休眠與隱形優化 (方案 A + 方案 B)
* **極致隱形與防止最小化 (方案 B)**：將 Gemini 自動合成彈出視窗大小縮小為 `200x200`，並精確定位在螢幕可用空間的右下角邊緣 (`availWidth - 50`, `availHeight - 50`)。這使視窗只有 50x50 像素露在螢幕內，其餘部分完全隱出螢幕外，既滿足了 Chrome 座標不被重設的安全限制，又防止了用戶手動最小化。
* **背景雙重防休眠保活 (方案 A)**：在油猴腳本 `gemini_auto_synth.user.js` 中新增了 `keepAliveWebRTC`（啟動虛擬 WebRTC 連線）以及 `startSilentAudio`（藉由使用者事件啟用靜音音頻播放）。這會使瀏覽器將該分頁視為高優先級的語音通訊與音訊活動分頁，從而完全免除瀏覽器因分頁最小化或置於背景而實施的 Tab Throttling CPU 節流限制，合成流程可持續在背景全速流暢執行。

---

## 🚀 最新更新項目 (2026-06-08 Web 推薦美學與圖片代理)

### 1. 本機伺服器圖片 CORS 代理與 Localhost 迴避死鎖機制
* **技術突破**：在 `vestis_server.py` 新增伺服器端圖片代理路由，當前端請求外部圖片時，伺服器會以標準 Chrome 瀏覽器之標頭代為抓取單品圖片，並在回傳時自動添加 `Access-Control-Allow-Origin: *`，徹底解決 CORS 畫布污染問題。
* **Localhost 代理迴避 (免除死鎖)**：在網頁端 `getProxiedImageUrl` 函數中，新增了 hostname 與 window.location 同源判斷。如果網址指向本地伺服器（如 `localhost` 或 `127.0.0.1`），**則不使用代理直接載入**。這防止了單線程 Python 伺服器在代理本地文件時引起的「自請求死鎖 (Deadlock)」，確保本地圖片可以順暢無阻地被 fetch 並成功轉成 Base64。

### 2. 智慧搜尋字詞自動簡化之導購連結
* **搜尋字詞簡化引擎**：為了解決 Uniqlo/GU 等官方電商平台的內建搜尋引擎因為商品名稱過長、過多修飾詞而導致「搜尋不到任何商品」的嚴重體驗缺陷，前端引入了 `getSimplifiedPurchaseLink` 核心過濾函數。
* **自動精簡核心詞**：跳轉至購買連結前，JavaScript 會自動將中文品名精簡為最核心的檢索詞（例如：將 `"極簡寬版高磅純棉 T 恤"` ➡️ 簡化為 `"T恤"`，`"直筒原色修身牛仔褲"` ➡️ `"牛仔褲"`，`"防風機能防潑水連帽外套"` ➡️ `"連帽外套"`），確保 Uniqlo、GU、蝦皮等平台 **100% 可以搜尋出相符的豐富商品列表**。

### 3. 本地化 90 款服裝數據集高精度單品庫
* **淘汰無關與風景照**：為解決先前使用 LoremFlickr/Flickr 下載時所產生的街景、無關照片（如海景、建築物、設計圖等）以及 placeholder 照片問題，**改用 `download_from_dataset.py` 直接對接 `clothing-dataset-small` 官方服飾學術數據集**。
* **100% 衣服單品平鋪/去背照**：從 GitHub 倉庫的 t-shirt/shirt/pants/outwear 目錄下，精確抓取了 90 張衣服單品特寫照片（上衣 30 張、下身 30 張、外套 30 張），成功儲存至本地 `穿搭照片/網站圖庫/Web推薦/` 資料夾中。
* **成果**：現在所有 90 張 Web 推薦照片已全數被置換為**無背景雜物、專注於服裝全貌特寫的去背或平鋪商品照**，完全去除了無關背景，精準度達到 100% 且大小無一重複，並在本地瞬間秒開載入，跨域渲染完美無阻。

### 4. 解決自動合成油猴 Userscript 崩潰 Bug
* **現象**：先前在拖曳 Web 推薦單品進行「一鍵多圖自動合成」時，網頁端因為自請求死鎖導致 `getBase64` 失敗而直接傳遞相對路徑 URL 給油猴腳本，造成 Userscript 內 `base64ToFile` 的 regex 匹配為 `null`，進而拋出 `Cannot read properties of null (reading '1')` 錯誤，卡死在「提示詞已輸入」。
* **防禦與修復**：
  - 除了避開 Localhost 代理死鎖以保證 Base64 Data URL 完美打包以外，
  - 我們在 `gemini_auto_synth.user.js` 的 `base64ToFile` 函數中加入了防禦性 `try-catch` 和資料前綴檢驗。如果資料格式並非 base64（例如 URL），會發出 Console.error 警告並回傳空 File 對象，**確保 Userscript 的自動合成流程能穩定跑完，再也不會卡死崩潰**。

---

## 🚀 次新更新項目 (2026-06-08 上流奢華兩欄式改版)

### 1. 本機伺服器圖片 CORS 與防阻擋代理 (`/api/proxy_image`)
* **技術突破**：在 `vestis_server.py` 新增伺服器端圖片代理路由，當前端請求外部圖片時，伺服器會以標準 Chrome 瀏覽器之標頭（User-Agent 與 Referer）代為抓取 Uniqlo、GU、蝦皮等網站的單品圖片，並在回傳時自動添加 `Access-Control-Allow-Origin: *`。
* **徹底解決 CORS 畫布污染**：解決了瀏覽器直接下載外站圖片時的跨來源資源共享（CORS）阻擋，確保前端 JavaScript 畫布（Canvas）在合併衣服或進行 `getBase64` 轉換時，不會因為「畫布污染 (canvas tainted)」而造成 VTON 渲染與合成失敗。

### 2. 穩定持久的官方搜尋導購連結
* **搜尋替代直連**：為解決 Uniqlo/GU 官方商品單品 URL 容易下架、過期失效的問題，改用官方搜尋 URL 連動商品名稱（Uniqlo TW 搜尋、GU TW 搜尋、蝦皮關鍵字搜尋），確保用戶點選「🛍️ 前往購買」時 100% 導向正確的商品購買頁面或類似款，永久有效。

### 3. 本機快取 90 款男性高美學時尚單品庫 (`webBatches` 本地化)
* **視覺重塑**：淘汰原先畫質粗糙、背景雜亂的開源服飾照片。
* **高解析精品男性相片**：重新精選 90 張 Unsplash 上具備高訂精品沙龍風格的男性服飾照（30 件上衣、30 件下著、30 件外套），完全切合男性模特兒主視覺。
* **本地化儲存 (零延遲、高穩定)**：將所有 90 張時尚照片直接下載至本機目錄 `穿搭照片/網站圖庫/Web推薦/` 下，前端直接透過相對路徑讀取本地相片，實現**網頁秒開、零加載延遲**，且百分之百避開防盜鏈阻擋與 CORS 畫布污染問題。
* **風格分類與品牌化**：標示為「Uniqlo」、「GU」、「蝦皮精選」品牌，全面融入 VESTIS ATELIER 高尚典雅的主題氛圍。

---

## 🚀 次新更新項目 (2026-06-08 上流奢華兩欄式改版)

### 1. 奢華品牌美學視覺重塑 (Luxury Brand Identity)
* **視覺調色盤**：捨棄原先科技霓虹感，全面改用代表高雅奢華的「香檳金 (`#c5a880`)」、「曜石黑 (`#070709`)」與「暖象牙白 (`#f1efea`)」交織的主題。
* **字型設定**：導入 Cormorant Garamond 襯線字體（應用於所有標題、品牌標誌、區塊標題）以及 Montserrat 無襯線字體（應用於內容文字、按鈕與數字），呈現如高訂沙龍 (Atelier/Maison) 的優雅質感。
* **光影細節**：將所有按鈕、卡片懸停、大頭貼框、彈窗邊框與 HUD 掃描動畫的陰影及邊框由亮藍/亮綠改為香檳金與柔和的金色霓虹效果。

### 2. 極簡兩欄式主視覺佈局 (Two-Column Grid System)
* **版面重構**：將原先三欄式版面（左側控制、中央展示、右側分身與日誌）收納為**左右兩欄**：
  * **左側多功能邊欄 (`360px`)**：內嵌奢華分頁切換按鈕（Atelier 衣櫃、Maison Twin 分身、Collection 珍藏），收納了原本左右側欄的所有功能。
  * **右側展示大畫面 (`col-main`)**：佔據所有剩餘寬度 (`1fr`)，將模特兒試穿畫面的視覺比例拉到最大，營造沉浸式的高訂體驗。
* **隱藏並安全保留 `.col-right`**：將 `.col-right` 設為 `display: none !important` 並保留空 DOM 以確保舊 JavaScript 程式碼在操作或抓取節點時不會因找不到元素而崩潰。

### 3. 多功能分頁整合 (Sidebar Tab Navigation)
* **👗 Atelier (衣櫃)**：原衣櫃單品清單、自訂上傳與左下角垃圾桶。
* **👤 Maison Twin (分身)**：原本位於右側的個人化數位分身設定卡片、AI 風格多維美學分析統計條。
* **✨ Collection (珍藏)**：原本位於右側的「我的收藏單品庫」與「歷史穿搭日誌」彈窗抽屜按鈕。
* **分頁切換器**：採用高質感半透明圓角膠囊設計，切換時伴隨滑順的金色主色過渡與陰影。

### 4. 手機版 RWD 導航調適 (Responsive Mobile Adaptation)
* **點擊連動切換**：修改手機版底部的浮動導航欄 (`mobileNav` & `initMobileLayout`)。當在手機版切換「衣櫃」、「分身」、「設定」時，自動切換左側邊欄對應的 Atelier、Maison Twin 與 Collection 分頁，並正常控制展示區與邊欄的顯示隱藏，確保手機版體驗完全無縫。

### 5. 試穿與合成報告狀態保持、AI 推薦替換試穿與導購連結
* **美學分析狀態連動**：設計了全域的 `currentMannequinOutfit` 狀態追蹤器。不論是手動拖曳單品還是點擊「AI 擬真渲染 (VTON) / 一鍵多圖自動合成」套用完成並清空單品覆蓋層後，系統皆能記住當前穿著，使得「展開完整報告」時始終能看到服飾組合的完整報告。
* **智慧推薦替換試穿與導購**：在分析報告的底部引進「✨ AI 美學替換推薦」模組。系統會自動推薦最適配的替代單品，該單品照片**支援直接拖曳至展示區進行試穿**，並新增 **「👗 立即試穿」** 互動按鈕。點擊後能一鍵置換模特兒身上該類別的單品並關閉報告彈窗，讓使用者無縫進行對比模擬，同時也提供直達 Google Shopping 的購買連結。

### 6. AI 隨機組合重塑為「AI美學穿搭建議」
* **功能升級與更名**：將原先純隨機選取單品的「AI隨機組合」按鈕重新命名為 **「AI 美學穿搭建議」**。
* **智慧穿搭美學演算法**：捨棄無序的隨機盲配，導入真正的風格協調演算法。系統點擊後會隨機選定一種穿搭主題：**雅痞紳士風 (Smart Casual)**、**街頭工裝風 (Streetwear Utility)** 或 **極簡日系風 (Minimalist City Boy)**。接著自動對衣櫃與推薦庫的單品進行分類特徵（襯衫、工裝、西裝、版型等）權重評分，篩選出風格一致 of 服飾進行整套協調搭配，並彈出 Toast 告知使用者當前推薦的主題，大幅提升實用性與設計質感。

### 7. 個人數位分身頭像臨時會話快取機制與垃圾桶 Bug 清理
* **臨時會話儲存 (sessionStorage)**：將原本永久儲存分身的 `localStorage` 機制改為 `sessionStorage`。當使用者開啟網頁時，分身頭像預設為「無人臉頭像 placeholder 👤」。當使用者上傳照片或相機拍攝並點選「建立分身」後，才會在當前分頁預存常駐，並更新首頁頭像做為人臉置換來源。一旦關閉分頁或瀏覽器後，該資料即被安全清除，重啟網頁時會乾淨地回到無人臉的預設狀態，提升隱私防護。
* **垃圾桶刪除崩潰修復**：修正了 `trash.ondrop` 中殘留的 `JSON.parse(saved)` 未定義引用錯誤，避免使用者拖曳非卡片物件（如本地檔案）至垃圾桶時造成 JS 程式碼崩潰。

---

## 🚀 次新更新項目 (2026-06-06 重大修復)

### 1. 補全並定義 `clearItemSilent()` 函數 (解崩潰 Bug)
* **問題**：原代碼在 Gemini 自動化合成完成後，會調用 `clearItemSilent` 清除主畫面遮罩，但該函數未被宣告，造成 JS 拋出未定義 ReferenceError 而在背景崩潰，導致合成圖片無法成功帶回主畫面。
* **修復**：正式定義了 `clearItemSilent(cat)` 函數，並在清除衣物後自動更新取消按鈕狀態與觸發風格分析（`updateCancelButtonsVisibility` & `analyzeStyles`）。

### 2. VTON 結果彈窗新增「✨ 套用至主畫面並收藏」功能
* **功能**：在渲染結果彈窗（`#vton-overlay`）中新增 **「✨ 套用至主畫面並收藏」** 按鈕，實作 `applyVTONResultToMain()`。點擊後一鍵將合成圖置換為主畫面模特兒，自動清除主畫面所有覆蓋單品，並同步下載及保存日誌，體驗大幅提升。

### 3. 多圖分傳防臉部失真 (Model Identity Preservation)
* **優化**：保留多圖分別下載上傳的架構（而非在前端拼貼成單張圖）。這可引導 Gemini 將上傳的模特兒原圖作為唯一人像錨點，**完美保留模特兒原有的臉部特徵，避免在 Image-to-Image 生成時導致臉部重繪失真**。

### 4. 模特兒底圖「乾淨隔離」機制 (解第二次合成衣服重疊 Bug)
* **修復**：修正了套用合成結果回主畫面時，合成衣服照覆寫數位分身底圖變數 `faceSwappedJpgUrl` 的問題。
* **邏輯**：將一鍵合成底圖獲取方式優化為：優先使用乾淨底圖 `faceSwappedJpgUrl`（有數位分身用分身），若無則使用乾淨預設模特兒 `modelFiles[currentModelIndex]`，而非帶有上次衣物的合成圖。解決了第二次一鍵合成時衣服重疊或沒穿新搭配單品的 Bug。

### 5. 重置自動化 Userscript 狀態鎖
* **修復**：在 `gemini_auto_synth.user.js` 的自動化流程 `automateWorkflow()` 開頭強制重置全域變數 `resultSent = false` 和 `initialResponseCount = 999`。解決在同一個 Tab 中多次執行一鍵自動合成時，Userscript 無法自動回傳圖片的問題。

### 6. 文字對話限制優化與去除指引面板
* **優化**：在 Gemini 提示詞開頭加入限制令 `【重要指令：不要輸出任何文字對話與解釋，直接生成並顯示穿搭合成圖片】`，命令 Gemini 跳過打字直接出圖，縮短了 3~5 秒的文字打字感官等待時間。
* **優化**：移除了開啟 Gemini 時在主畫面彈出的引導指引面板，簡化操作流程。

### 7. 一鍵多圖自動合成「隱形背景執行 + HUD 高科技終端進度」
* **優化**：當點擊「一鍵多圖自動合成」後：
  1. 系統會立刻啟動 **HUD 掃描動畫 (Laser Overlay)** 與 **高科技日誌終端 (HUD Terminal)**，其感官與 Classic VTON 擬真渲染完全一致。
  2. **徹底隱藏彈窗搶鏡**：將開啟的 Gemini 視窗規格設為極小（`width=100,height=100`）並將座標開在電腦螢幕解析度之外（`left=3000,top=3000`），同時呼叫 `win.blur()` 失去焦點並將主頁 `window.focus()` 召回，實現網頁端完全沒有新視窗彈跳搶奪螢幕的無感背景執行體驗。
  3. **實時防阻擋安全檢測**：加入瀏覽器彈出式視窗攔截檢測，若被攔截會自動彈出人性化的 Toast 防呆警告。
  4. **自動生命週期銷毀**：待背景 Gemini 自動合成並將結果傳回後，網頁端主畫面會自動換裝，並**在背景主動執行 `activeGeminiWindow.close()` 徹底銷毀該小視窗**以釋放系統資源，隨後關閉 HUD 掃描動畫，提供極佳的無縫體驗。

---

## 🌟 核心功能升級

### 1. 全新 VTON 友善 Web 推薦資料庫
為徹底解決原先 Uniqlo/GU 官方圖片防爬蟲阻擋（404 錯誤），以及 Unsplash 實境照片背景雜亂導致無法進行虛擬穿戴（VTON）去背渲染的技術瓶頸，我們全面升級了底層資料庫。
- **導入真實隔離服飾資料集**：改用語義分割開源專案（clothing-dataset-small）的真實服飾照片。
- **確保 VTON 完美渲染**：所有 90 件單品皆為單件、隔離且乾淨背景的高品質圖片，完全符合虛擬穿戴引擎的嚴苛要求。

### 2. 獨立 90 件單品與真實「換一批」功能
修復了之前三個批次皆顯示重複照片的邏輯錯誤。
- **上衣、下著、外套** 各自擴充至 30 件完全不同、絕不重複的單品。
- **一鍵換一批**：使用者點擊「🔄 換一批」時，能真實輪替並瀏覽全新的 10 件單品（共 3 批次循環）。

### 3. 「全部」分類頁面修復
修復了點選「全部」時畫面一片空白的異常，現在系統能聰明地將「上衣、下著、外套、鞋款、配件」五大分類合併渲染於同一個畫面，實現「合併顯示 10 件」的無縫瀏覽體驗。

### 4. 📸 全新功能：一鍵多圖快速合成試穿 (Mimo 特色)
針對使用者想將多件不同衣物一次合成到指定人像身上的需求，我們新增了「一鍵多圖合成」專區：
- **玻璃擬態 UI 彈窗 (`#quick-synth-overlay`)**：在展示區上方新增「📸 一鍵多圖合成」按鈕，點擊可開啟支援毛玻璃高質感磨砂底色的彈窗。
- **四大合成區塊**：
  1. **人像基底 (必填)**：支援直接點選「套用目前」載入當前展示區的模特兒（自動壓縮為 Base64 格式），亦可點擊上傳自訂人體照片。
  2. **上衣照片 (必選)**：提供上著單品檔案。
  3. **下著照片 (選填)**：提供下身褲子/裙子單品檔案。
  4. **鞋款/配件 (選填)**：提供鞋子或配件單品檔案。
- **多圖合成技術架構**：
  * **平行 VTON 渲染**：上衣與下著會同時發送 AI 請求至 fal.ai 引擎，使合成等待時間縮短 50%。
  * **智能腰部分割縫合**：調用 `mergeOutfitCanvas()` 將兩個渲染完畢的模特兒照片（上衣與下身）於腰部（Y軸 52% 處）進行自然漸層融合。
  * **鞋款 Canvas 自動拼貼**：若上傳了鞋款，系統將呼叫 `overlayShoesOnCanvas()` 自動以比例拼貼至模特兒腳部區域（Y軸 82% 處）。
  * **快取 Base64**：所有渲染結果與上傳檔案皆即時快取為 Base64 格式，避免 CDN 連結失效及 CORS 限制。

---

## 🛠️ 互動邏輯與重大 Bug 修復

### 1. 展示區（模特兒）拖曳渲染與一鍵合成拖曳
- **拖曳接收邏輯**：重寫展示區的 `ondrop` 接收邏輯。優先搜尋個人衣櫃，若無則自動比對全部的 Web 推薦資料庫 (`webBatches`)。
- **合成區拖曳支援**：全新一鍵多圖合成的 4 個上傳方框全面支援 HTML5 Drag & Drop。使用者可以直接將衣櫃內、WEB 推薦區的卡片，甚至本地電腦資料夾中的圖片直接拖入框中，系統會自動辨識並載入（包含類別防呆警告）。
- **點擊即上傳**：點擊合成區的任何方框可直接觸發檔案瀏覽器，互動更人性化。

### 2. 垃圾桶（移除單品區）錯誤渲染修復
- **問題描述**：使用者將單品拖曳至左下角的「移除單品區」時，不但無法刪除，反而會觸發試穿渲染。
- **修復方式**：分離並精確修正了垃圾桶專屬的 `trash.ondrop` 事件。
- **成果**：
  - 拖曳「個人上傳的單品」至垃圾桶：正常觸發刪除功能。
  - 拖曳「內建預設單品」或「Web 推薦單品」：系統會啟動防呆機制，彈出「⚠️ 內建/Web 推薦單品無法刪除」的提示，且絕對不會再誤觸發渲染動作。

### 3. 個人衣櫃相機與數位分身系統全面恢復
- **問題描述**：重構拖曳防呆邏輯時意外導致相機存取與數位分身建立按鈕失效。
- **修復方式**：
  - 重新綁定並隔離相機影像擷取邏輯，確保 `webcam` 與 `face-webcam` 的 HTML DOM ID 準確對應。
  - 完美復原「大頭照人臉置換 (head)」與「全身照替換假人 (full)」雙模態設定的精確儲存與讀取流程。

### 4. 單品詳情「前往購買」導向
考量到瀏覽器的 CORS 安全限制，點擊單品詳情的「🛍️ 前往購買」按鈕時，系統會自動捕捉單品類別與名稱，並開啟 Google Shopping 進行精準搜尋，既確保了試穿圖片的高品質，也保留了完整流暢的導購體驗。

---

## 📱 行動版 RWD 佈局與效能優化

### 1. 手機端單欄式佈局與 Floating Nav Bar
- **單欄式結構**：在寬度小於 768px 的裝置上，三欄式版面自動切換為縱向流式排版，各面板（衣櫃、試穿、分身）按需隱藏以防畫面過於擁擠。
- **底部浮動導航欄**：新增底部選單，點選「衣櫃、試穿、分身、設定」可動態切換視圖展示，並平滑滾動至頁首。

### 2. CSS 語法滲漏與重複區塊清理
- **修復問題**：移除了先前版本中因多次覆蓋而在 `<body>` 開頭和 `<div class="app-shell">` 之間殘留的巨大 CSS 重複洩漏區塊，使 HTML 結構恢復乾淨合規。

### 3. 觸控手勢防殘影設計
- **無懸停觸控**：利用 `@media (hover: none) and (pointer: coarse)` 停用手機端的 Hover 縮放與陰影特效，解決移動端點擊卡片後出現「特效殘留」的視覺干擾。
